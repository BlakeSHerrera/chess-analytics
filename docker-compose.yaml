services:
  extract:
    build: .
    command: python extract/extract.py
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
    stdin_open: true
    tty: true
    volumes:
      - chess_data:/app/data

volumes:
  chess_data:
