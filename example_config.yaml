# --- Gateway Connection ---
gateways:
  duckdb:
    connection:
      # For more information on configuring the connection to your execution engine, visit:
      # https://sqlmesh.readthedocs.io/en/stable/reference/configuration/#connection
      # https://sqlmesh.readthedocs.io/en/stable/integrations/engines/duckdb/#connection-options
      type: duckdb
      catalogs:
        chess_analytics:  # Path to .db file goes here
      # concurrent_tasks: 1
      # register_comments: True
      # pre_ping: False
      # pretty_sql: False
      # schema_differ_overrides: 
      # catalog_type_overrides: 
      # catalogs: 
      # extensions: 
      # connector_config: 
      # secrets: 
      # filesystems: 
      # token: 
    state_connection:
      type: duckdb
      database:  # Path to .db file goes here


default_gateway: duckdb

# --- Model Defaults ---
# https://sqlmesh.readthedocs.io/en/stable/reference/model_configuration/#model-defaults

model_defaults:
  dialect: duckdb
  start: 2013-01-01  # Start date for backfill history
  cron: '@monthly'   # Run models monthly at 12am UTC (can override per model)

# --- Variables ---

variables:
  datalake:  # Path to folder goes here

# --- Linting Rules ---
# Enforce standards for your team
# https://sqlmesh.readthedocs.io/en/stable/guides/linter/

linter:
  enabled: true
  rules:
    - ambiguousorinvalidcolumn
    - invalidselectstarexpansion
    - noambiguousprojections
